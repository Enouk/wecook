'use strict';

/**
 * @ngdoc function
 * @name wecookApp.controller:ChefsCtrl
 * @description
 * # ChefsCtrl
 * Controller of the wecookApp
 */
angular.module('wecookApp')
  .controller('ChefsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
